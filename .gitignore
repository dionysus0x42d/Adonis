# 先忽略全部
*

# 允許 gitignore 自己被追蹤
!.gitignore

# 允許要備份的檔案
!app.py
!config.py
!requirements.txt
!CLAUDE.md
!gvdb_schema.sql
!README.md

# 允許 static / templates 與其底下所有內容
!static/
!static/**

!templates/
!templates/**

# 允許 view_only 靜態網站（用於 GitHub Pages）
!view_only/
!view_only/**

# 允許 scripts 目錄（用於資料匯出等工具）
!scripts/
!scripts/**
